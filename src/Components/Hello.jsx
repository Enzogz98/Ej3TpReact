import React from 'react'

// eslint-disable-next-line react/prop-types
const Hello = ({name}) => {
  return (
    <div>
      <p>
        Hello, {name}
      </p>
    </div>
  )
}

export default Hello
